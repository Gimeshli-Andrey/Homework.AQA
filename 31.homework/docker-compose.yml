services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      # Если вам нужно добавить том с конфигурациями, раскомментируйте следующую строку:
       - /Users/gimeshli.a/Desktop/Homework.AQA/31.homework/jenkins_compose_data/jenkins_compose/jenkins_configuration:/jenkins_compose_data/jenkins_compose/jenkins_configuration
    environment:
      - DISPLAY=:99
    ports:
      - "8080:8080"
