pipeline {
    agent any

    stages {
        // Крок для вилучення коду з репозиторію
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        // Крок для встановлення залежностей проекту
        stage('Install Dependencies') {
            steps {
                sh '''
                    python3 -m pip install --user --upgrade pip
                    python3 -m pip install --user -r requirements.txt
                '''
            }
        }

        // Крок для запуску автотестів
        stage('Run Tests') {
            steps {
                sh 'python3 -m pytest --alluredir=allure-results --disable-warnings'
            }
        }

        // Крок для збору результатів тестування та їх публікації у Jenkins
        stage('Publish Test Results') {
            steps {
                allure([
                    includeProperties: false,
                    jdk: '',
                    properties: [],
                    reportBuildPolicy: 'ALWAYS',
                    results: [[path: 'allure-results']]
                ])
            }
        }
    }

    post {
        always {
            // Надсилання результатів тестування на email
            emailext (
                to: 'InsertYour@Mail.Here',
                subject: "Build ${currentBuild.currentResult}: Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]'",
                body: """
                    <p>Статус збірки: ${currentBuild.currentResult}</p>
                    <p>Номер збірки: ${env.BUILD_NUMBER}</p>
                    <p>Переглянути результати: ${env.BUILD_URL}</p>
                    <p>Звіт тестування Allure: ${env.BUILD_URL}allure</p>
                """,
                mimeType: 'text/html',
                attachLog: true
            )
        }
    }
}