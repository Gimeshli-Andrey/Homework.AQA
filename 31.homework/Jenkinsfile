pipeline {
    agent {
        docker {
            filename 'Dockerfile'
            args '-v $PWD:/app'
        }
    }

    environment {
        EMAIL_RECIPIENT = 'gimeshli.a@gmail.com'
    }

    stages {
        stage('Install python') {
            steps {
                sh 'apt-get install -y python3 python3-dev '
                sh 'apt install -y python3-pip '
                sh 'rm -rf venv '
                sh 'apt install -y python3.11-venv '
                sh 'python3 -m venv venv '
            }
        }

        stage('Install dependencies') {
            steps {
                sh '''#!/bin/bash
                source venv/bin/activate
                pip install -r requirements.txt'''
            }
        }

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                sh '''#!/bin/bash
                source venv/bin/activate
                pip install --upgrade pip
                pip install -r requirements.txt'''
            }
        }

        stage('Run Tests') {
            steps {
                sh '''#!/bin/bash
                source venv/bin/activate
                python3 -m pytest --alluredir=allure-results --disable-warnings --junitxml=test-results.xml'''
            }
            post {
                always {
                    junit 'test-results.xml'
                }
            }
        }

        stage('Publish Test Results') {
            steps {
                allure([
                    includeProperties: false,
                    reportBuildPolicy: 'ALWAYS',
                    results: [[path: 'allure-results']]
                ])
            }
        }
    }

    post {
        always {
            emailext (
                to: "${EMAIL_RECIPIENT}",
                subject: "Build ${currentBuild.currentResult}: Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]'",
                body: """
                    <p>Статус сборки: ${currentBuild.currentResult}</p>
                    <p>Номер сборки: ${env.BUILD_NUMBER}</p>
                    <p>Просмотреть результаты: ${env.BUILD_URL}</p>
                    <p>Отчет тестирования Allure: ${env.BUILD_URL}allure</p>
                """,
                mimeType: 'text/html',
                attachLog: true
            )
        }
        cleanup {
            deleteDir()
        }
    }
}